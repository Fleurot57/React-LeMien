import './SignIn.css';

//Roger
//Title
//Formulaire : input(Nom), Input(prénom), input(mail), input(mdp)
//Button (SignIn)

function SignIn() {
  return (
    <div>
    </div>
  );
}

export default SignIn;
