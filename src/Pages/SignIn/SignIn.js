import './SignIn.css';

//Roger
//Title
//Formulaire : input(Nom), Input(prénom), input(mail), input(mdp)
//Button (SignIn)

function SignIn() {
  return (
    <div>
      <p>Inscription</p>
    </div>
  );
}

export default SignIn;
